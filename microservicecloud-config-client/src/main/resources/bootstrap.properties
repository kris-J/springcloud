# config server的地址
spring.cloud.config.uri=http://localhost:3344
# config server的application名称,多个文件使用逗号分隔，有先后顺序，写在后面的会覆盖前面文件相同名字的属性值
spring.cloud.config.name=application-demo,microservicecloud-config-cient
# config server的分支
spring.cloud.config.label=master